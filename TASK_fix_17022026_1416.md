# System Health Fix Plan - 17/02/2026

This document tracks the resolution of failures identified by the `master-test-runner.js`.

## Failure Analysis & Fix Proposals

| Error | Cause | Fix Proposal | File Name |
| :--- | :--- | :--- | :--- |
| `TypeError: ... (reading 'create')` on `db.refreshToken` | `db.refreshToken` is not initialized in the mock client. | Add `refreshToken` to the mock Prisma client setup. | `backend/tests/helpers/mockPrismaClient.js` |
| `TypeError: ... (reading 'create')` on `db.usedPartLog` | `db.usedPartLog` missing in the mock or service reference. | Add `usedPartLog` to the mock Prisma client and verify service import. | `backend/tests/helpers/mockPrismaClient.js` |
| `TypeError: db.transferOrder.count is not a function` | `count` method of Prisma models is missing from the mock helper. | Implement `count: jest.fn()` in the `mockPrismaClient` helper. | `backend/tests/helpers/mockPrismaClient.js` |
| `TypeError: app.address is not a function` | The `app` being tested is likely an uninitialized or incorrectly exported express object. | Ensure `server.js` exports a valid `app` and the test uses it correctly. | `backend/tests/security/branch-isolation.test.js` |
| `TypeError: ... (reading 'aggregate')` in `metricsCache` | `db[model].aggregate` is missing in certain contexts. | Add `aggregate: jest.fn()` to mock client and check for null references. | `backend/services/metricsCache.js` |
| `toHaveProperty('monthly')` failed | Dashboard response structure changed (flattened or renamed). | Update test assertions to match current `revenue` object structure. | `backend/tests/integration.test.js` |
| `Missing required fields` vs `No items to transfer` | Validation error message updated in service but not test. | Align the expected error string in the test with the service's implementation. | `backend/services/transfer/orderService.js` |
| Argument mismatch in `updateMany` | Arguments ordered differently or extra fields passed (e.g., `branchId`). | Update test expectations to match the actual logic in `transferService`. | `backend/tests/services/transferService.test.js` |
| `Order not found` systemic error | Mock `findUnique` not handling `_skipBranchEnforcer` or ID lookup correctly. | Refine mock behavior for extended Prisma queries and ensure valid IDs. | `backend/services/transfer/orderService.js` |
| `customerBkcode` property mismatch | `maintenanceRequest` creation now includes extra fields. | Update test `toHaveBeenCalledWith` expectations to include new fields. | `backend/tests/services/requestService.test.js` |
| `status` mismatch: `UNDER_MAINTENANCE` | Service status transitions changed in recent refactor. | Update test to expect `UNDER_MAINTENANCE` instead of `EXTERNAL_REPAIR`. | `backend/tests/services/requestService.test.js` |
| `Can't read data of zip file` | Mock `Workbook` implementation doesn't support `load`. | Improve `exceljs` mock to provide better behavior for load simulations. | `backend/tests/services/transferService.smoke2.test.js` |
| API Integrity (Phase 3) Failure | Live server connectivity is required for this phase. | Ensure local server is started or provided with a fallback live URL. | `master-test-runner.js` |

## Fix Progress Checklist

- [ ] **Infrastructure & Mocks**
  - [ ] Fix `mockPrismaClient.js` (add `refreshToken`, `usedPartLog`, `count`, `aggregate`).
  - [ ] Fix `branch-isolation.test.js` (app address issue).
- [ ] **Service Logic & Messages**
  - [ ] Align `transferService` error messages.
  - [ ] Fix `transferService` update/creation argument mismatches.
  - [ ] Resolve `Order not found` behavior in `orderService.js`.
- [ ] **Data Structure & API**
  - [ ] Update `integration.test.js` (Dashboard revenue properties).
  - [ ] Update `requestService.test.js` (new fields/status transitions).
- [ ] **Advanced Features**
  - [ ] Fix `MetricsCache` aggregate calls.
  - [ ] Fix `exceljs` mock behavior.
- [ ] **Master Runner**
  - [ ] Add server-start check or documentation for Phase 3.
